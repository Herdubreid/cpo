<Page
    x:Class="Celin.Pages.Browse"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Celin.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <CommandBar Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton Icon="Find" Label="Find Orders" />
            <AppBarButton Icon="OpenFile" Label="Select Order" />
            <AppBarButton Icon="Add" Label="Add Order" />
            <CommandBar.Content>
                <TextBlock Text="Informational text"/>
            </CommandBar.Content>
        </CommandBar>
        <TextBlock Grid.Row="1">Welcome to Browse</TextBlock>
    </Grid>
</Page>
